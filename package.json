{
  "name": "sum-insured-impact-react",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run build && wrangler deploy",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy dist",
    "preview": "vite preview",
    "test": "vitest"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "read-excel-file": "^9.0.10"
  },
  "devDependencies": {
    "@testing-library/react": "^14.0.0",
    "vite": "^6.4.2",
    "vitest": "^4.1.7",
    "wrangler": "^4.95.0"
  }
}
